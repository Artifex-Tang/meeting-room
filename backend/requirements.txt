# backend/requirements.txt
# 固定到次版本，避免上游小版本破坏行为

# Web 框架
fastapi==0.115.*
uvicorn[standard]==0.32.*

# 数据校验
pydantic==2.9.*
pydantic-settings==2.6.*

# ORM & 迁移
SQLAlchemy==2.0.*
alembic==1.13.*
PyMySQL==1.1.*
cryptography==43.*           # PyMySQL 的 caching_sha2_password 需要

# 认证
python-jose[cryptography]==3.3.*
passlib[bcrypt]==1.7.*
bcrypt==4.2.*

# HTTP 客户端（调微信 code2session + 订阅消息 API）
httpx==0.27.*

# Redis
redis==5.1.*

# 调度器（订阅消息"即将开始"定时推送）
APScheduler==3.10.*

# 日期工具（周期展开）
python-dateutil==2.9.*

# 限流（可选）
slowapi==0.1.*

# ---------- dev / test ----------
pytest==8.3.*
pytest-asyncio==0.24.*
pytest-mock==3.14.*
httpx[http2]==0.27.*        # TestClient 依赖
freezegun==1.5.*            # 时间相关测试（调度器、取消时限）
black==24.*
ruff==0.7.*
